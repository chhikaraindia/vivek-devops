<?php
/**
 * DEBUG CONFIGURATION FOR VIVEK DEVOPS
 *
 * Add these lines to your wp-config.php file (before "That's all, stop editing!"):
 *
 * // Enable WP_DEBUG mode
 * define('WP_DEBUG', true);
 *
 * // Enable Debug logging to the /wp-content/debug.log file
 * define('WP_DEBUG_LOG', true);
 *
 * // Disable display of errors and warnings (logs to file instead)
 * define('WP_DEBUG_DISPLAY', false);
 * @ini_set('display_errors', 0);
 *
 * // Use dev versions of core JS and CSS files (only needed if you are modifying these core files)
 * define('SCRIPT_DEBUG', true);
 *
 * After adding these, check: /wp-content/debug.log for detailed error messages
 */
